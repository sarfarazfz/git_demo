import 'package:flutter/material.dart';

class FragmentAddNew extends StatelessWidget {
  const FragmentAddNew({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("I am Add New fragment"),
    );
  }
}
