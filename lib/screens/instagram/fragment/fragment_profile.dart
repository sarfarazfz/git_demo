import 'package:flutter/material.dart';

class FragmentProfile extends StatelessWidget {
  const FragmentProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("I am Profile fragment"),
    );
  }
}
