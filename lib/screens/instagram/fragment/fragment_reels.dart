import 'package:flutter/material.dart';

class FragmentReels extends StatelessWidget {
  const FragmentReels({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("I am Reels fragment"),
    );
  }
}
