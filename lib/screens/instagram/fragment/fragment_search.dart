import 'package:flutter/material.dart';

class FragmentSearch extends StatelessWidget {
  const FragmentSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("I am Search fragment"),
    );
  }
}
